<div class="panel">
    <h3><i class="icon icon-gift"></i> {$smarty.const._PS_MODULE_DIR_} Gift Card Management</h3>
    {if isset($content)}
        {$content}
    {else}
        <p>No gift cards found.</p>
    {/if}
</div>
